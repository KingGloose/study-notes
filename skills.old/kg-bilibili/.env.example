# B 站登录态 cookie。推荐直接跑 `python scripts/login.py` 扫码自动生成本文件。
# 也可手动：浏览器 F12 → Application → Cookies → bilibili.com 复制。
# SESSDATA 有效期约一个月，过期重新扫码即可。本文件不含真实凭证，.env 才是。
SESSDATA=
BILI_JCT=
BUVID3=
DEDEUSERID=
