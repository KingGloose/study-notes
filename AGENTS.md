# AGENTS.md · LLM Wiki 维护契约

这是一个按 Andrej Karpathy 的 **LLM Wiki** 模式运作的个人知识库。
本文件告诉 AI(你)如何维护这个库。每次开始工作前先读这份契约,再读 `index.md` 定位。

---

## 核心哲学

1. **AI 时代,知识库的价值不是存储通用知识**(那是 AI 的活),而是:
   - **唤醒**:让主人知道"某个知识点存在",从而能主动去用、去问 AI、去判断 AI 的回答对不对。
   - **沉淀**:只保存 AI 给不出的东西——个人判断、项目上下文、踩坑、决策、独特理解。

2. **存量整体归档,向前建新库**。旧笔记全部在 `archive/`,原样封存,不主动处理。

3. **价值靠使用浮现**。不预先判断哪篇旧笔记有价值。只有当主人**实际查阅到**某篇旧内容、且它确实有用时,才 just-in-time 把它蒸馏/核对/升级成正式 `wiki/` 页。

4. **不铺开处理**。不批量转图片、不批量核对旧笔记、不批量蒸馏。一切按需。

---

## 目录结构

```
学习笔记/
├── AGENTS.md      # 本文件:维护契约
├── index.md       # 知识点唤醒索引(广而全,含通用知识关键词)
├── log.md         # 流水账(append-only)
│
├── raw/           # 原始资料(只读):B站转写、博客/公众号原文、重要 AI 对话存档
├── assets/        # 新库图片池(从零累积,与旧 assets 隔离)
├── wiki/          # AI 沉淀的知识,内部按领域分子目录(前端/ai/python/...)
│
└── archive/       # 旧世界整体封存:旧笔记 + 旧 00 assets + 旧领域目录
                   # 图谱默认过滤 -path:archive 隐藏
```

## 三层对应(Karpathy 模型)

- **raw** = 原始资料层(不可变,source of truth)
- **wiki** = LLM 维护的知识层(你写,主人读)
- **AGENTS.md + index.md + log.md** = schema + 导航基础设施

---

## 写作约定(沿用主人的旧风格)

- 中文为主。
- 标题层级用 `# 1.` `## 1.1` `### 1.1.1` 风格(带编号)。
- 图片:新内容的图放 `assets/`,引用用 Obsidian 语法 `![[assets/文件名]]`。
- 代码用 ```` ```语言 ```` 围栏。
- **优先文字和代码,少截图**。旧库最大的问题就是知识困在图片里搜不到、AI 读不到。

---

## 操作流程

### Ingest(摄入新资料)

1. 资料先存 `raw/`(B站转写、文章原文、对话记录),原样保留可追溯。
2. 读它,和主人讨论要点。
3. **判断**:抽掉主人的个人上下文后,这内容 AI 还能完整答出来吗?
   - 能(纯通用知识)→ 只在 `index.md` 补一个知识点关键词(唤醒),不写详细页。
   - 不能(有个人判断/项目上下文/踩坑)→ 写成 `wiki/` 对应领域的知识页。
4. 更新 `index.md`,追加 `log.md` 一条。

### Query(查询)

- 回答"我学过哪些 X"类问题:先读 `index.md` 定位,再读具体页(wiki 或 archive)。
- **明确区分信息来源**:哪些是"笔记里记过的",哪些是"我(AI)补充的通用知识"。不要混为一谈。
- 好的查询答案(对比、分析、梳理)如果有长期价值,file 回 `wiki/` 成为新页,别让它消失在对话里。

### 按需升级(just-in-time,旧笔记 → wiki)

当某篇 archive 旧笔记被实际用到且值得升级时:
1. 读原笔记(必要时读图,把图里的关键知识转成文字/代码)。
2. 用知识 + 必要时联网,**核对**过时/错误的点,明确标注("当年对,Vue 3 后变了" / "此说法不准确,应为 X")。
3. 补充新进展。
4. 生成干净的新版放进 `wiki/`,主人确认后入库。
5. **核对结果是"建议"不是定论**——AI 也会错,最终由主人判断拍板。

### Lint(定期体检,可选)

- 找 wiki 里的矛盾、过时结论、孤儿页、缺失的交叉引用。
- 建议可以联网补充的数据缺口和值得深入的新问题。

---

## 图片处理规则(按需,三档)

- **A 档·知识型**(代码截图、配置、命令):读图转成文字/代码块,让知识可搜索。
- **B 档·效果型**(IDE 界面、运行效果):保留图,补一行文字说明它展示了什么。
- **C 档·装饰型**(AI 随手能讲的通用示意图):忽略,不投入。
- 只在某页被实际升级时才处理图片,不批量处理旧库 6000+ 张图。

---

## 图谱(Obsidian)

- 归档已完成。图谱默认过滤 `-path:archive`,只显示新库知识网。
- 维护 `wiki/` 时**主动建双链** `[[...]]`,让知识点真正关联,而不是靠目录堆砌。
- 旧库靠目录组织,几乎没有双链;新库要建成"活的知识网络"。

---

## 禁止事项

- 不要去动 `archive/` 里的旧笔记内容(除非在做按需升级,且升级产物放 wiki,不改原文)。
- 不要批量处理(转图/核对/蒸馏)——一切按需。
- 不要把 AI 能直接答的通用知识写成详细 wiki 页——那种只进 index 做唤醒。
- 不要在回答里把"笔记记过的"和"AI 补充的"混在一起不加区分。
